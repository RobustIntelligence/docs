# ri/api/imageregistry/image_registry.proto
## Version: version not set

### /v1/images

#### GET
##### Summary:

ListImages

##### Description:

List all images that match a specified set of constraints.

[Python pagination example](#tag/JobReader/operation/JobReader_ListJobs)

##### Parameters

| Name | Located in | Description | Required | Schema |
| ---- | ---------- | ----------- | -------- | ---- |
| pageToken | query | Specifies a page of the list returned by a ListImages query. The ListImages query returns a pageToken when there is more than one page of results. | No | string |
| pageSize | query | The maximum number of Image objects to return in a single page. | No | string (int64) |

##### Responses

| Code | Description | Schema |
| ---- | ----------- | ------ |
| 200 | A successful response. | [rimeListImagesResponse](#rimelistimagesresponse) |
| default | An unexpected error response. | [googlerpcStatus](#googlerpcstatus) |

#### POST
##### Summary:

CreateImage

##### Description:

Creates a new managed image with a unique name.

##### Parameters

| Name | Located in | Description | Required | Schema |
| ---- | ---------- | ----------- | -------- | ---- |
| body | body |  | Yes | [rimeCreateImageRequest](#rimecreateimagerequest) |

##### Responses

| Code | Description | Schema |
| ---- | ----------- | ------ |
| 200 | A successful response. | [rimeCreateImageResponse](#rimecreateimageresponse) |
| default | An unexpected error response. | [googlerpcStatus](#googlerpcstatus) |

### /v1/images/list

#### POST
##### Summary:

ListImages

##### Description:

List all images that match a specified set of constraints.

[Python pagination example](#tag/JobReader/operation/JobReader_ListJobs)

##### Parameters

| Name | Located in | Description | Required | Schema |
| ---- | ---------- | ----------- | -------- | ---- |
| body | body |  | Yes | [rimeListImagesRequest](#rimelistimagesrequest) |

##### Responses

| Code | Description | Schema |
| ---- | ----------- | ------ |
| 200 | A successful response. | [rimeListImagesResponse](#rimelistimagesresponse) |
| default | An unexpected error response. | [googlerpcStatus](#googlerpcstatus) |

### /v1/images/{name}

#### GET
##### Summary:

GetImage

##### Description:

Returns the definition of a specified managed image.

##### Parameters

| Name | Located in | Description | Required | Schema |
| ---- | ---------- | ----------- | -------- | ---- |
| name | path | Name of existing image. | Yes | string |

##### Responses

| Code | Description | Schema |
| ---- | ----------- | ------ |
| 200 | A successful response. | [rimeGetImageResponse](#rimegetimageresponse) |
| default | An unexpected error response. | [googlerpcStatus](#googlerpcstatus) |

#### DELETE
##### Summary:

DeleteImage

##### Description:

Deletes a specified managed image.

##### Parameters

| Name | Located in | Description | Required | Schema |
| ---- | ---------- | ----------- | -------- | ---- |
| name | path | Name of existing image. | Yes | string |

##### Responses

| Code | Description | Schema |
| ---- | ----------- | ------ |
| 200 | A successful response. | [rimeDeleteImageResponse](#rimedeleteimageresponse) |
| default | An unexpected error response. | [googlerpcStatus](#googlerpcstatus) |

### Models


#### ImageReferenceReferenceType

 - REFERENCE_TYPE_RIME_DOCKERHUB: Signifies an external image that is located in RIME's Docker Hub
repository.
 - REFERENCE_TYPE_MANAGED: Signifies an image managed by the managed image registry.
 - REFERENCE_TYPE_EXTERNAL_ECR: Signifies an external image located in an ECR repository.
 - REFERENCE_TYPE_EXTERNAL_GAR: Signifies an external image located in a GAR repository.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| ImageReferenceReferenceType | string |  - REFERENCE_TYPE_RIME_DOCKERHUB: Signifies an external image that is located in RIME's Docker Hub repository.  - REFERENCE_TYPE_MANAGED: Signifies an image managed by the managed image registry.  - REFERENCE_TYPE_EXTERNAL_ECR: Signifies an external image located in an ECR repository.  - REFERENCE_TYPE_EXTERNAL_GAR: Signifies an external image located in a GAR repository. |  |

#### ListImagesRequestPipLibraryFilter

Specification of a filter for a pip library.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| name | string | Name of the library. | No |
| version | string |  | No |

#### ManagedImagePackageRequirement

A requirement specifying a system package to be installed on the image.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| name | string | Name of the package. | No |
| versionSpecifier | string |  | No |
| packageType | [ManagedImagePackageType](#managedimagepackagetype) | The type of system package, which specifies the package manager to use to install this package. | No |

#### ManagedImagePackageType

The type of package manager used to install system packages.

 - PACKAGE_TYPE_UNSPECIFIED: The default type that corresponds to an unspecified type error.
 - PACKAGE_TYPE_APT: Specifies packages from the apt package manager available in
distributions of Debian Linux.
 - PACKAGE_TYPE_ROCKYLINUX: Specifies packages from the yum package manager available in
distributions of Rocky Linux.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| ManagedImagePackageType | string | The type of package manager used to install system packages.   - PACKAGE_TYPE_UNSPECIFIED: The default type that corresponds to an unspecified type error.  - PACKAGE_TYPE_APT: Specifies packages from the apt package manager available in distributions of Debian Linux.  - PACKAGE_TYPE_ROCKYLINUX: Specifies packages from the yum package manager available in distributions of Rocky Linux. |  |

#### ManagedImagePipLibrary

A pip library to install on the managed image.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| name | string | Name of the library. | No |
| version | string | Version of the library. | No |

#### ManagedImagePipRequirement

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| name | string | Name of the library. | No |
| versionSpecifier | string | Specifier for a version of the library, see: https://www.python.org/dev/peps/pep-0440/#version-specifiers or https://peps.python.org/pep-0440/ for reference. | No |

#### ManagedImageRoleType

RoleType signifies the role the image plays within the registry and
the permissions users have for executing actions on the image.

 - ROLE_TYPE_UNSPECIFIED: The role type was not specified when the image was created, as is
the case with legacy images. Treated as the `USER_DEFINED` role type.
 - ROLE_TYPE_SYSTEM: The system role indicates that the image is generated and used by the
managed image system and is critical to image building
functionality. These images cannot be modified by users.
System-level images are generated by the managed image system and are
critical for building other images.
 - ROLE_TYPE_USER_DEFINED: The user-defined role indicates that the image was created by users
and that users can execute arbitrary actions on it.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| ManagedImageRoleType | string | RoleType signifies the role the image plays within the registry and the permissions users have for executing actions on the image.   - ROLE_TYPE_UNSPECIFIED: The role type was not specified when the image was created, as is the case with legacy images. Treated as the `USER_DEFINED` role type.  - ROLE_TYPE_SYSTEM: The system role indicates that the image is generated and used by the managed image system and is critical to image building functionality. These images cannot be modified by users. System-level images are generated by the managed image system and are critical for building other images.  - ROLE_TYPE_USER_DEFINED: The user-defined role indicates that the image was created by users and that users can execute arbitrary actions on it. |  |

#### TestRunProgressTestBatchProgress

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| type | string | (test run ID, test batch type) are a unique ID of the test batch. | No |
| status | [rimeTestTaskStatus](#rimetesttaskstatus) | The status of each test batch is populated by the synthesizer. | No |

#### googlerpcStatus

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| code | integer |  | No |
| message | string |  | No |
| details | [ [protobufAny](#protobufany) ] |  | No |

#### protobufAny

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| @type | string |  | No |

#### rimeArchivedJobLogs

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| url | [rimeSafeURL](#rimesafeurl) |  | No |
| expirationTime | dateTime |  | No |

#### rimeContinuousTestJobProgress

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| testRuns | [ [rimeContinuousTestRunProgress](#rimecontinuoustestrunprogress) ] |  | No |

#### rimeContinuousTestRunProgress

ContinuousTestRunProgress is a wrapper around TestRunProgress with
added metadata about the bins.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| testRun | [rimeTestRunProgress](#rimetestrunprogress) |  | No |
| binStartTime | dateTime |  | No |
| binEndTime | dateTime |  | No |

#### rimeCreateImageRequest

Request and response for a CreateImage RPC.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| name | string | Name of the image. | Yes |
| pipRequirements | [ [ManagedImagePipRequirement](#managedimagepiprequirement) ] | List of `pip` requirements that specify the customization used for this image. | Yes |
| packageRequirements | [ [ManagedImagePackageRequirement](#managedimagepackagerequirement) ] | List of system requirements that specify the customization used for this image. | No |
| pythonVersion | string |  | No |

#### rimeCreateImageResponse

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| image | [rimeManagedImage](#rimemanagedimage) |  | No |
| job | [rimeJobMetadata](#rimejobmetadata) | Details for the image builder job that was launched. | No |

#### rimeDeleteImageResponse

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| rimeDeleteImageResponse | object |  |  |

#### rimeEnsureImageExistenceResponse

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| rimeEnsureImageExistenceResponse | object |  |  |

#### rimeGetImageResponse

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| image | [rimeManagedImage](#rimemanagedimage) |  | No |

#### rimeImageReference

A reference to another image, managed or not.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| name | string | Name of the image being referenced. For a managed image, this is the `name` record of the `ManagedImage` object. For an external image, this is the full name of that image. | No |
| referenceType | [ImageReferenceReferenceType](#imagereferencereferencetype) | The type of image being referenced. | No |

#### rimeJobData

Note that progress may not be populated depending on the parameters of the
request.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| stress | [rimeJobDataStressTest](#rimejobdatastresstest) |  | No |
| continuousInc | [rimeJobDataContinuousIncrementalTest](#rimejobdatacontinuousincrementaltest) |  | No |
| fileScan | object |  | No |

#### rimeJobDataContinuousIncrementalTest

A continuous incremental test job runs `n` test runs, where `n` is the
number of bins specified in the dataset.
This is a production specific workflow and is used to monitor a model's
performance over time.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| firewallId | string |  | No |
| ctTestRunIds | [ string ] |  | No |
| progress | [rimeContinuousTestJobProgress](#rimecontinuoustestjobprogress) |  | No |

#### rimeJobDataStressTest

A stress test job runs a single test run over a model and dataset.
This is also known as offline testing; it is run before production
deployment of the model.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| projectId | [rimeUUID](#rimeuuid) |  | No |
| testRunId | string |  | No |
| progress | [rimeStressTestJobProgress](#rimestresstestjobprogress) |  | No |

#### rimeJobMetadata

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| jobId | string | The identifier within our job tracking system. | No |
| terminationReason | [rimeTerminationReason](#rimeterminationreason) |  | No |
| jobType | [rimeJobType](#rimejobtype) | The type of the job. | No |
| status | [statedbJobStatus](#statedbjobstatus) | The job's current status. | No |
| startTime | dateTime | The start time of the job (when the job transitions in state to RUNNING). Note, this may not be populated immediately when the job is created. | No |
| creationTime | dateTime | The time the job was created. | No |
| runningTimeSecs | double | The total running time a job took to complete if the job is finished or the current running time if the job is still in progress (seconds). | No |
| jobData | [rimeJobData](#rimejobdata) | Job-type specific data. e.g. for stress test jobs, this is populated with the project ID and the test run ID. The type of the job data payload must match the JobType in this message. | No |
| jobProgressStr | string | Pretty-printed, human-readable representation of job progress. This will only be populated for Read methods with the FULL job view. To get schema for progress for each type of job, see the field inside that job's job data message (e.g. StressTestJobProgress). Note: this is unstable, do not rely on parsing this. | No |
| cancellationRequested | boolean | Marked when the job has been requested to be cancelled by the user. This is declarative; once the user requests cancellation, the backend will conduct garbage collection on the job in the background and eventually update the status of the job to CANCELLED. | No |
| agentId | [rimeUUID](#rimeuuid) | agent_id denotes the agent this job is run on. | No |
| archivedJobLogs | [rimeArchivedJobLogs](#rimearchivedjoblogs) |  | No |

#### rimeJobType

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| rimeJobType | string |  |  |

#### rimeListImagesRequest

Request and response for a ListImages RPC.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| pageToken | string | Specifies a page of the list returned by a ListImages query. The ListImages query returns a pageToken when there is more than one page of results. | No |
| pageSize | string (int64) | The maximum number of Image objects to return in a single page. | No |
| pipLibraries | [ [ListImagesRequestPipLibraryFilter](#listimagesrequestpiplibraryfilter) ] | Optional. Constrains which `pip` libraries are installed on the image. The filter will only be active if the list is nonempty. If specified, do not include a pageToken field in the request. | No |

#### rimeListImagesResponse

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| images | [ [rimeManagedImage](#rimemanagedimage) ] |  | No |
| nextPageToken | string |  | No |

#### rimeManagedImage

A representation of a custom image whose tag or version is managed by the
ImageRegistry.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| name | string | The external name of the image. This name must match the /^[a-z][a-z0-9]*(?:[_-][a-z0-9]+)*$/ regular expression. See the naming rules in https://docs.docker.com/engine/reference/commandline/tag/#extended-description  protolint:disable:this MAX_LINE_LENGTH https://docs.aws.amazon.com/AmazonECR/latest/userguide/repository-create.html  protolint:disable:this MAX_LINE_LENGTH from which the naming convention derives. The above names are valid ECR or Docker image names. | No |
| baseImage | [rimeImageReference](#rimeimagereference) | A reference to the source image on which this image is built. | No |
| roleType | [ManagedImageRoleType](#managedimageroletype) | The role type of the image. | No |
| childImages | [ [rimeImageReference](#rimeimagereference) ] | The set of images that use this image as a source. | No |
| rimeTag | string | The tag of the RIME wheel used to build the managed image. | No |
| repoUri | string | The URI of the repository. | No |
| status | [rimeManagedImageStatus](#rimemanagedimagestatus) | Status of the managed image. | No |
| packageRequirements | [ [ManagedImagePackageRequirement](#managedimagepackagerequirement) ] | A list of all system package requirements used to build this image. | No |
| pipRequirements | [ [ManagedImagePipRequirement](#managedimagepiprequirement) ] | A list of all pip requirements used to build this image. | No |
| pipLibraries | [ [ManagedImagePipLibrary](#managedimagepiplibrary) ] | A list of all pip libraries installed on this image as obtained by running `pip list`. | No |
| pythonVersion | string | The version of Python used to build the RIME image. | No |

#### rimeManagedImageStatus

- STATUS_BUILDING_FIRST_TIME: The building states distinguish initial
builds from builds to synchronize a managed image to the most recent
RIME version. This distinction is important because initial builds
have a higher tendency to encounter errors.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| rimeManagedImageStatus | string | - STATUS_BUILDING_FIRST_TIME: The building states distinguish initial builds from builds to synchronize a managed image to the most recent RIME version. This distinction is important because initial builds have a higher tendency to encounter errors. |  |

#### rimeSafeURL

SafeURL represents a URL that has been safely constructed.
e.g. a user clicking on this link is guaranteed to land on the RIME web app.
We use this instead of a raw str so that application code can generate
SafeURL messages in certain ways -> this provides stronger typing than just
using a string.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| url | string | A safely constructed URL. | No |

#### rimeStressTestJobProgress

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| testRun | [rimeTestRunProgress](#rimetestrunprogress) |  | No |

#### rimeSyncImageTagResponse

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| image | [rimeManagedImage](#rimemanagedimage) |  | No |
| job | [rimeJobMetadata](#rimejobmetadata) |  | No |

#### rimeTerminationReason

 - TERMINATION_REASON_OOMKILLED: The job was terminated for using more memory than the amount assigned
to the job.
 - TERMINATION_REASON_GENERIC_ERROR: This is the catch-all error and is typically used for user errors.
 - TERMINATION_REASON_INTERNAL_ERROR: Specifies a system error. Contact the support team.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| rimeTerminationReason | string |  - TERMINATION_REASON_OOMKILLED: The job was terminated for using more memory than the amount assigned to the job.  - TERMINATION_REASON_GENERIC_ERROR: This is the catch-all error and is typically used for user errors.  - TERMINATION_REASON_INTERNAL_ERROR: Specifies a system error. Contact the support team. |  |

#### rimeTestRunProgress

TestRunProgress is a shared message for representing the progress of a
single test run.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| testRunId | string |  | No |
| status | [rimeTestTaskStatus](#rimetesttaskstatus) |  | No |
| testBatches | [ [TestRunProgressTestBatchProgress](#testrunprogresstestbatchprogress) ] |  | No |

#### rimeTestTaskStatus

Status of individual test units, e.g. test runs, test batches, test cases.
This is different from job status because these are more abstract.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| rimeTestTaskStatus | string | Status of individual test units, e.g. test runs, test batches, test cases. This is different from job status because these are more abstract. |  |

#### rimeUUID

Unique ID of an object in RIME.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| uuid | string | String value of the object ID. | No |

#### rimeUpdateBuildInfoResponse

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| rimeUpdateBuildInfoResponse | object |  |  |

#### statedbJobStatus

TODO(AIRS-764): redesign as part of Job Monitoring consolidation with 
StateDB effort.

 - JOB_STATUS_PENDING: Resources have been created for the job but the job has not started yet.
 - JOB_STATUS_FAILED: Blanket status for user or system-related job failure.
Check the Termination Reason for details.
 - JOB_STATUS_REQUESTED: The job descriptor exists but has no resources allocated.
Jobs that remain in this status without moving to the PENDING status are
at risk of entering the FAILED status.
 - JOB_STATUS_CANCELLED: Job has been cancelled.
Cancelled jobs cannot be recovered.
 - JOB_STATUS_RECONCILING: Job is being reconciled by the operator.

| Name | Type | Description | Required |
| ---- | ---- | ----------- | -------- |
| statedbJobStatus | string | TODO(AIRS-764): redesign as part of Job Monitoring consolidation with  StateDB effort.   - JOB_STATUS_PENDING: Resources have been created for the job but the job has not started yet.  - JOB_STATUS_FAILED: Blanket status for user or system-related job failure. Check the Termination Reason for details.  - JOB_STATUS_REQUESTED: The job descriptor exists but has no resources allocated. Jobs that remain in this status without moving to the PENDING status are at risk of entering the FAILED status.  - JOB_STATUS_CANCELLED: Job has been cancelled. Cancelled jobs cannot be recovered.  - JOB_STATUS_RECONCILING: Job is being reconciled by the operator. |  |